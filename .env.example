# ParkHub Rust — Environment Variables
# Copy this file to .env (optional — these can also be passed to docker compose directly)
# All variables have sane defaults for single-node Docker deployments.

# Server binding
PARKHUB_HOST=0.0.0.0
PARKHUB_PORT=8080

# Change this before first start! Default admin password on first boot.
# Username is always "admin". Log level: debug, info, warn, error
PARKHUB_ADMIN_PASSWORD=admin
RUST_LOG=info

# Database encryption at rest (optional — leave empty to disable AES-256-GCM)
# PARKHUB_DB_PASSPHRASE=change-me-to-a-strong-passphrase

# TLS (optional — leave empty to use plain HTTP)
# PARKHUB_TLS_ENABLED=true
# PARKHUB_TLS_CERT=/data/cert.pem
# PARKHUB_TLS_KEY=/data/key.pem

# SMTP email (optional — leave empty to disable email notifications)
# PARKHUB_SMTP_HOST=smtp.example.com
# PARKHUB_SMTP_PORT=587
# PARKHUB_SMTP_USER=user@example.com
# PARKHUB_SMTP_PASS=secret
# PARKHUB_SMTP_FROM=noreply@example.com
